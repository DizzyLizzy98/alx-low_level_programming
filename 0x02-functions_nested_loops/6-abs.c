#include "main.h"

/**
* _abs - Function that compute absolute value of int
* @n: the int to check
* Return: absolute value of int
*/

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
